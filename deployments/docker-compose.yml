version: '3.8'
services:
  web:
    image: nginx:alpine
    ports:
    - 80:80
    volumes:
    - ./html:/usr/share/nginx/html
  api:
    build: .
    ports:
    - 8000:8000
    environment:
      DATABASE_URL: postgresql://localhost/db
